services:
  data-query-worker:
    init: true
    build:
      context: .
    image: omero-data-query-worker:smoke
    environment:
      DQW_API_TOKEN: smoke-test-token-at-least-16-characters
      DQW_CACHE_DIR: /var/lib/omero-data-query-worker
      TMPDIR: /var/lib/omero-data-query-worker/tmp
      DQW_DUCKDB_MEMORY_LIMIT: 512MB
      DQW_DUCKDB_THREADS: "2"
      DQW_MAX_CONCURRENT_QUERIES: "4"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 128
    mem_limit: 2g
    cpus: 2.0
    tmpfs:
      - /tmp:size=256m,mode=1777
    volumes:
      - data-query-cache:/var/lib/omero-data-query-worker
    networks:
      - worker-private
    restart: unless-stopped

networks:
  worker-private:
    internal: true

volumes:
  data-query-cache:
